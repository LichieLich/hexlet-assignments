en:
  activerecord:
    models:
      # BEGIN
      comment:
        zero: No comments
        one: 1 comment
        other: '%{count} comments'
      # END
      post/comment: Comment
    attributes:
      # BEGIN
      post:
        body: Body
        title: Title
      # END
      post/comment:
        body: Body

    errors:
      models:
        post:
          attributes:
            # BEGIN
            body:
              too_long: is above 500 chars
            title:
              blank: really can't be blank
            # END
